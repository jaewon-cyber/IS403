<!DOCTYPE html>
<html>
<head>
    <title>Your Profile</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <style>
        :root {
            --byu-navy: #002e5d;
            --byu-silver: #a7a8aa;
            --byu-white: #ffffff;
            --byu-accent: #0a4a8a;
        }

        body {
            background: linear-gradient(135deg, var(--byu-navy), var(--byu-accent));
            background-size: 200% 200%;
            animation: gradientShift 8s ease infinite;
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: var(--byu-navy);
            margin: 0;
            padding: 0;
            overflow-x: hidden; /* prevent horizontal scroll */
        }


        @keyframes gradientShift {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        .profile-card {
            background: var(--byu-white);
            padding: 2rem;
            width: 600px;
            border-radius: 1.25rem;
            box-shadow: 0 8px 20px rgba(0,0,0,0.15);
            border-top: 8px solid var(--byu-navy);
        }

        .class-box {
            background: #f8f9fa;
            border-left: 4px solid var(--byu-navy);
            padding: 0.75rem;
            border-radius: 0.5rem;
            margin-bottom: 0.5rem;
        }

        .page-container {
            max-width: 700px;
            margin: 2rem auto; /* center horizontally, spacing on top/bottom */
            padding: 1rem;
        }
        
        .btn-custom {
            background-color: var(--byu-navy) !important; /* make sure it overrides Bootstrap */
            color: var(--byu-white) !important;
            border: none;
            border-radius: 0.5rem;
            padding: 0.6rem 1.5rem;
            font-size: 1rem;
            margin: 0.5rem 0;
            transition: background-color 0.2s ease;
        }

        .btn-custom:hover {
            background-color: var(--byu-accent) !important;
        }

    </style>
</head>

<body>
<div class="page-container">
  <div class="welcome-card">
    <div class="profile-card">
        <h2 class="text-center mb-3">
            <i class="bi bi-person-circle me-2"></i>
            <%= student.stud_first_name %> <%= student.stud_last_name %>
        </h2>
        <p><strong>Email:</strong> <%= student.stud_email %></p>
        <p><strong>Phone:</strong> <%= student.stud_phone_number %></p>
        <p><strong>Gender:</strong> <%= student.stud_gender %></p>
        <p><strong>Age:</strong> <%= student.stud_age %></p>

        <hr>

        <h5>Enrolled Courses</h5>

        <% if (classes.length === 0) { %>
            <p class="text-muted">No courses found.</p>
        <% } else { %>
            <% classes.forEach(c => { %>
                <div class="class-box">
                    <strong><%= c.subject_code %> <%= c.course_number %></strong><br>
                    <span><%= c.semester %> <%= c.year %></span>
                </div>
            <% }) %>
        <% } %>

        <div class="text-center mt-3">
            <a href="/editProfile" class="btn btn-custom btn-primary me-2">Edit Profile</a>
            <a href="/" class="btn btn-secondary">Back</a>
        </div>
     </div>
    </div>
</div>
</body>
</html>

